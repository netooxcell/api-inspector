DATABASE_PATH=./requests.db
HOST=0.0.0.0
PORT=8000
DEFAULT_TIMEOUT=10

# Real backend to transparently reverse-proxy /capture/* traffic to (see README "Capturing real site traffic")
UPSTREAM_BASE_URL=http://localhost:3000
